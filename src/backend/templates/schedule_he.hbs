<!DOCTYPE html>
<html lang="he">
<head>

  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

  <meta name="description" content="{{ apptitle_he }}">
  <meta name="author" content="{{ metaauthor }}">

  <title>{{ apptitle_he }}</title>

  <!-- Bootstrap core CSS -->
  <!-- Latest compiled and minified CSS -->
  <link rel="shortcut icon" href="{{ eventurls.ico_url }}" type="image/x-icon" />
  {{> fonts}}
  <link rel="stylesheet" href="./dependencies/bootstrap.min.css">
  <link rel="stylesheet" href="./css/schedule_he.css">
  <link rel="manifest" href="./manifest.json">
  <link rel="apple-touch-icon" href="./images/icon.png"/>

  {{> analytics}}

  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
      <![endif]-->
</head>

<body dir="rtl">
  <a id="top"></a>
  {{> navbar_he otherpage='schedule'}}

  <div class="main container" id="session-list">
    <div class="row">
      <div class="middle col-sm-12">
        <h2 class="filter-heading track-heading text-center">
        <span>
          תוכנית
        </span>
        </h2>
      </div>
    </div>

    <div class="date-list container">
      <div class="tabs">
        <span class="tabs-nav">
          {{#timeList}}
          <a class="tabs-nav-link" href="./schedule_he.html#{{slug}}"> {{date_short_he}} </a>
          {{/timeList}}
        </span>
      </div>
    </div>

    <div class="row">
      <div style="margin: 15px 0 0 5px;" class="col-md-9 col-rtl">
        <div class="track-list container">

          {{#timeList}}
            <div style="padding:0" class="col-md-12">
              <a class="anchor" id="{{slug}}"></a>
              <h4 class="text">{{date_he}}</h4>
            </div>
            {{#times}}

            <div class="row">
              <div class="eventtime col-xs-2 col-sm-2 col-md-2 col-rtl">
                <h4>{{caption}}</h4>
              </div>
              <div class="tracks col-xs-10 col-sm-10 col-md-10 col-rtl">
                {{#sessions}}
                {{#unless is_cancelled}}
                <div class="track-inline">
                  <a class="anchor" id="{{session_id}}"></a>
                  <div style="padding-right:0px;" class="schedule-container">
                    <div id="title-{{session_id}}" class="sizeevent event" style="background-color:{{{color}}}"
                      data-toggle="collapse" data-target="#desc-{{session_id}}, #desc2-{{session_id}}">
                      <h4>
                        {{#if is_cancelled}}
                        <del>{{title_he}}</del>
                        {{else}}                       
                        {{title_he}}
                        {{/if}}                       
                      </h4>
                      <a id="bookmark-{{session_id}}" class="bookmark">
                        <i class="fa fa-star" aria-hidden="true"></i>
                      </a>
                      <div id="desc2-{{session_id}}" class="collapse in">
                        <div class="row speaker-short-info">
                          <h5>
                          {{#speakers_list}}
                            {{name_he}}{{#if @last}}{{else}},{{/if}}
                          {{/speakers_list}}
                          </h5>
                          <h6>
                          {{#if language_he}}
                          <i class="fa fa-globe" aria-hidden="true"></i>&nbsp;{{language_he}}
                          {{/if}}
                          {{#if location_he}}
                          {{#if language_he}}&nbsp;&nbsp;&nbsp;{{/if}}<i class="fa fa-map-marker-alt" aria-hidden="true"></i>&nbsp;{{location_he}}
                          {{/if}}
                          {{#if shabbat}}
                          &nbsp;&nbsp;&nbsp;<i class="fa fa-menorah" aria-hidden="true"></i>&nbsp;שומר שבת
                          {{/if}}
                          </h6>
                        </div>
                      </div>
                    </div>
                    <div id="desc-{{session_id}}" class="collapse">
                      <p><i style="color:{{{color}}}" class="fa fa-circle" aria-hidden="true"></i>&nbsp;{{tracktitle_he}}</p>
                      {{#if language_he}}
                      <p><i class="fa fa-globe" aria-hidden="true"></i>&nbsp;{{language_he}}</p>
                      {{/if}}
                      {{#if location_he}}
                      {{#if location_color}}
                      <p><i style="color:{{{location_color}}}" class="fa fa-map-marker-alt" aria-hidden="true"></i>&nbsp;<i>{{location_he}}</i></p>
                      {{else}}
                      <p><i class="fa fa-map-marker-alt" aria-hidden="true"></i>&nbsp;<i>{{location_he}}</i></p>
                      {{/if}}
                      {{/if}}
                      {{#if shabbat}}
                      <p><i class="fa fa-menorah" aria-hidden="true"></i>&nbsp;שומר שבת</p>
                      {{/if}}
                      <hr class="clear-both">
                      {{#if description_he}}
                      <p>
                        <span class="tip-description">
                          {{{description_he}}}
                        </span>
                      </p>
                      <hr class="clear-both">
                      {{/if}}
                      <div class="session-speakers-list">
                        {{#each speakers_list}}
                        <a href="speakers_he.html#{{id}}">
                          <p class="session-speakers">
                          {{#if thumb}}
                            <img onError="this.onerror=null;this.src='dependencies/avatar.png';" class="card-img-top speaker-image-small" src="{{{thumb}}}"/>
                          {{else}}
                            <img class="card-img-top speaker-image-small" src="dependencies/avatar.png"/>
                          {{/if}}
                          </p>
                          <p><span class="graytext ">{{name_he}}</span></p>
                        </a>
                        {{#if organisation_he}}
                        <p>
                          {{organisation_he}}
                        </p>
                        {{/if}}
                        {{#if short_biography_he}}
                        <p>
                          <i>{{short_biography_he}}</i>
                        </p>
                        {{/if}}
                        {{#if long_biography_he}}
                        <p>
                          {{long_biography_he}}
                        </p>
                        {{/if}}
                        <hr class="clear-both">
                        {{/each}}
                        <div class="blacktext">
                          <p>{{sessiondate_he}}, <span>{{start}} - {{end}}</span></p><br />
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
                {{/unless}}
                {{/sessions}}
              </div>
            </div>
            {{/times}}
          {{/timeList}}

        </div>
      </div>
      <div class="track-names">
      {{#tracknames}}
      {{#if title_he}}
        <ul class="title-inline title-legend">
          <li  style="background-color:{{{color}}}" class="titlecolor"></li>
          <li>{{title_he}}</li>
        </ul>
      {{/if}}
      {{/tracknames}}
      </div>
    </div>
  </div>

  {{> footer_he}}

  <script type="text/javascript">
    $(document).ready(function() {
      $('.bookmark').click(function(e){
        var temp = JSON.parse(localStorage["{{eventurls.name}}"]);
        var id = Number($(this).attr('id').substring(9));
        var curColor = $(this).css("color");
        if (curColor == "rgb(255, 255, 0)") {
          $(this).css("color", "rgb(127, 127, 127)");
          temp[id] = 0;
        }
        else {
          $(this).css("color", "rgb(255, 255, 0)");
          temp[id] = 1;
        }
        localStorage["{{eventurls.name}}"] = JSON.stringify(temp);

        e.stopPropagation();
      });

      function initPage() {
        if(localStorage.hasOwnProperty("{{eventurls.name}}") === false) {
          localStorage["{{eventurls.name}}"] = '{}';
        }

        var temp = JSON.parse(localStorage["{{eventurls.name}}"]);
        Object.keys(temp).forEach(function(key) {
          if (temp[key] == 1) {
            $('#bookmark-' + key).css("color", "rgb(255, 255, 0)");
          }
        });
      }
      initPage();
    });
  </script>

</body>
</html>
